// CaptureKit — system-audio (Core Audio process tap) + microphone capture.
// Intentionally empty until its slice (M1 slice 3); types are defined with
// real knowledge then, not speculatively here.
